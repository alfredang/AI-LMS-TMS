# Dependencies
node_modules/
client/node_modules/
server/node_modules/

# Next.js build artifacts
.next/

# Production build
out/
dist/
build/

# Environment variables
.env.local
.env.example

# Local settings (sensitive config, not uploaded to GitHub)
config/local-settings.json
service-account.json

# Backlog (local planning file, not shared)
.project/backlog.md

# Nemo AI memory (runtime-generated)
data/nemo-memory.md

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# nyc test coverage
.nyc_output

# Coverage directory used by tools like istanbul
coverage/

# Dependency directories
jspm_packages/

# TypeScript cache
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Microbundle cache
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variables file
.env

# Local SSG certificates / keys (never commit)
certs/

# parcel-bundler cache (https://parceljs.org/)
.cache
.parcel-cache

# next.js build output
.next

# nuxt.js build output
.nuxt

# vuepress build output
.vuepress/dist

# Serverless directories
.serverless

# Stores VSCode versions used for testing VSCode extensions
.vscode-test

# Database dumps
*.dump
*.backup

# Temporary files
temp/
tmp/

# Upload directories (large files)
# These Two Files Size Is Too Big, So I Will Not Upload Them To Github
public/uploads/slides/*
public/uploads/guides/*
public/uploads/training_provider/private_key/*
public/uploads/training_provider/self_signing_cert/*
public/uploads/training_provider/ssg_app1_cert/*
public/uploads/training_provider/ssg_app1_private_key/*
public/uploads/training_provider/service_account/*
# Claims import uploads (and any other runtime uploads)
public/uploads/claims/*
# Generated course banner PNGs (uploaded to R2 from there)
public/uploads/course-banners/*
public/uploads/**/*.pdf
public/uploads/**/*.xlsx
public/uploads/**/*.xls
# server/public/uploads/
# client/public/uploads/

# Old structure (keep until migration verified)
# Uncomment these after verifying migration success:
# client/
# server/
GOOGLE_KEY_B64.txt

# Visibility (managed by /toggle-visibility)
CLAUDE.md
.mcp.json
# Exclude .claude CONTENTS (use /* not / so specific children can be re-included)
.claude/*
!.claude/CLAUDE.md
!.claude/skills/
# Shareable project tooling (sanitized — no secrets/infra specifics)
!.claude/agents/
!.claude/commands/
!.claude/hooks/
!.claude/settings.json
# Private (stay ignored): .claude/settings.local.json, .claude/security-findings.md, .claude/scheduled_tasks.lock
.project/

# Google Apps Script backups
*.gs

# n8n workflow exports (local artifacts)
n8nworkflows/
n8nworkflows.md/
.n8n/

# Playwright MCP / CLI session artifacts (logs + page snapshots)
.playwright-cli/
.playwright-mcp/
.version

# Tenant-specific seed data (contains plaintext credentials)
database/seeds/tertiary-software-credentials.sql

# Personal ad-hoc scripts — never commit (contained live DB creds; see incident 2026-07-01)
scratch/

# Local-only binary artifacts (not for the public repo)
docs/AI-LMS-TMS-Pitch-Deck.pptx
eraser-ring-dashed.png

# Distribution archives (rebuildable — never pushed to GitHub)
*.zip

# Source material derived from third parties — not ours to republish.
**/reference/
# Our own n8n workflow doc — a project artifact, keep it tracked.
!docs/reference/
!docs/reference/**

# Raw TPGateway financial transaction reports — real learner PII (names, NRIC, payment refs).
transaction data/
